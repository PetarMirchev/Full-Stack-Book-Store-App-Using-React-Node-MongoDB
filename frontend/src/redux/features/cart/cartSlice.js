import { createSlice } from '@reduxjs/toolkit'

const initialState = {
    cartItems: [],
    value: 0,
}


const createSlice = createSlice({
    name: 'cart',
    initialState: initialState,
    reducers: {

    }
})


